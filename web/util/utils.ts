export function getTokenUri(tokenURIPath: string) {
    return `https://ipfs.io/ipfs/${tokenURIPath}`;
}

// const tokenURIPath = await ticketNFTContract.tokenURI(i.tokenId);
// const tokenURI = `https://ipfs.io/ipfs/${tokenURIPath}`;